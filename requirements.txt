# Нужно только для ЛР №7 и №8 (MPI). ЛР №1–6 используют лишь стандартную библиотеку.
# Предварительно установите MPI-реализацию:
#   macOS:          brew install open-mpi
#   Debian/Ubuntu:  sudo apt install -y openmpi-bin libopenmpi-dev
numpy>=1.24
mpi4py>=3.1
